{
  "name": "tradepilot-ai-scanner",
  "version": "2.0.0-rebuild",
  "private": true,
  "description": "Frontend-only rebuild shell for TradePilot AI Scanner.",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend -- --port 8000",
    "check": "npm run check --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend -- --port 8000"
  },
  "engines": {
    "node": "22.x"
  }
}
